---
title: Підключення до OpenCode
description: Виправте проблему, коли PiChamber не підключається до свого сервера OpenCode.
---

# Підключення до OpenCode

Якщо PiChamber завантажується, але ніяк не виходить за межі стану «OpenCode перезапускається», або чат не відповідає, сервер, із яким він спілкується, недоступний. Пройдіться по цьому.

## Він застряг на «OpenCode перезапускається»

- дайте йому хвилину одразу після запуску — цей стан нормальний, поки сервер стартує
- перевірте, чи сервер живий, командою `pichamber status`
- перезапустіть його командою `pichamber restart`
- перегляньте деталі запуску командою `pichamber logs`

## Ви підключаєтеся до власного сервера

Якщо ви налаштували PiChamber на використання наявного сервера, ще раз перевірте налаштування з [OpenCode Server](/uk/opencode-server/):

- `OPENCODE_HOST` має містити порт, без шляху — наприклад, `http://localhost:4096`
- задайте `OPENCODE_SKIP_START=true`, щоб PiChamber не запускав ще й свій
- якщо адреса недійсна, PiChamber ігнорує її й натомість запускає власний сервер — шукайте попередження `[config]` у журналах

## Усе ще не працює

- переконайтеся, що версія Node.js — `20` або новіша
- перевстановіть найновіший CLI
- відкрийте `http://localhost:3000` напряму, перш ніж тестувати будь-який тунель чи віддалене посилання

## Пов'язане

- [OpenCode Server](/uk/opencode-server/) — як PiChamber знаходить сервер і керує ним
- [Усунення несправностей](/uk/troubleshooting/) — інші поширені проблеми
