---
title: Agent Control Tool
description: Let an agent manage PiChamber sessions and worktrees from chat.
---

# Agent Control Tool

Use the `pichamber` agent tool to manage work in the app directly from chat. It is enabled by default when PiChamber runs its own local OpenCode server; there is no separate tool to install or shell command to run.

## What you can ask

Ask the agent in plain language. For example:

- "Create a new PiChamber session in this project, use the `openai/gpt-5.6-sol` model, and send it this prompt: review the authentication flow."
- "Create a new PiChamber session for this task in a separate worktree, and ask it to add tests for the login flow."
- "Use PiChamber to list my 10 most recent sessions and include their current status."
- "Check the PiChamber session named Authentication review and show me its latest assistant response."


## Keep in mind

- New session prompts return immediately by default. Follow the session in PiChamber, or ask the agent to check it later.
- A separate worktree is only created when you ask for one. Uncommitted changes from your current worktree are not copied into it.
- The tool cannot delete sessions or worktrees, register project paths, run arbitrary shell commands, or call arbitrary URLs.

## Turn the tool on or off

Open **Settings → General → OpenCode CLI**, change **Agent control tool**, then select **Save + Reload**. The setting applies after the managed OpenCode server restarts.

The tool is not available when PiChamber connects to an external OpenCode server through `OPENCODE_HOST` or skip-start, or inside the VS Code extension. Desktop and web installations that use PiChamber's managed OpenCode server support it automatically.

## Related

- [Worktree Sessions](/worktrees/)
